# Server Configuration
PORT=5000

# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster_url> with your actual MongoDB Atlas credentials
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/corebiz?retryWrites=true&w=majority

# JWT Token Secret Key
JWT_SECRET=your_jwt_secret_key_here
